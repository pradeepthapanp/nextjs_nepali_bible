/**
 * Barrel for the Authentication feature domain types.
 *
 *   auth.ts   AuthState, AuthStatus, AuthSession, AuthUser, UserProfile,
 *             AuthProviderId, AuthDeepLink, SignupStatus
 */

export * from "./auth";
