/**
 * Bible Search feature — barrel.
 *
 * Imports the whole search module: components, hooks, queries, stores, types
 * and utils. Everything reuses the shared Bible layer (search service, React
 * Query hooks, parser, renderer, interaction + selection systems) — nothing is
 * duplicated here.
 */
export * from "./components";
export * from "./hooks";
export * from "./queries";
export * from "./store";
export * from "./types";
export * from "./utils";
