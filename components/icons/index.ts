/**
 * Shared icon registry — the ONLY place feature icons are defined.
 * Feature navigation/icons use Font Awesome via `FeatureIcon`; Lucide stays
 * for generic UI controls. Import `FeatureIcon`/`FeatureIconName` from here
 * (or the `@/components/icons` alias).
 */

export * from "./feature-icons";
